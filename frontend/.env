# URL de base du backend. Laisser vide (ou "/api") pour utiliser un
# chemin relatif via un reverse-proxy en production.
# En développement local, pointe vers le serveur FastAPI (uvicorn).
VITE_API_URL=http://localhost:8000
